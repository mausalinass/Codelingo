VITE_API_BASE_URL=http://localhost:5080
# Production: use your HTTPS API origin, or leave empty for a same-origin reverse proxy.
# Demo controls are hidden in production by default. No secrets belong in VITE_* settings.
